animations:
  # Animation settings for pure window translations (position changes only).
  window_move:
    enabled: true
    duration_ms: 150
    # Easing function. Named options: 'linear', 'ease_in', 'ease_out',
    # 'ease_in_out', 'ease_in_cubic', 'ease_out_cubic', 'ease_in_out_cubic',
    # 'ease_out_spring'. Custom cubic bezier: 'cubic_bezier(x1, y1, x2, y2)'
    # where x1/x2 must be in [0, 1]; y1/y2 may exceed that range for
    # overshoot. Examples:
    #   "cubic_bezier(0.16, 1, 0.3, 1)"  - expo-out (fast start, smooth stop)
    #   "cubic_bezier(0.4, 0, 0.2, 1)"   - Material Design standard
    easing: "cubic_bezier(0.2, 0.0, 0.0, 1.0)"
    # Minimum pixel distance required to trigger movement animations.
    # Helps prevent animations from starting on very small position changes.
    # Increase this value on high-DPI displays to reduce sensitivity.
    # Default: 10
    threshold_px: 10

  # Animation settings for window resize operations.
  window_resize:
    enabled: true
    duration_ms: 150
    # See 'window_move.easing' for available options.
    easing: "cubic_bezier(0.2, 0.0, 0.0, 1.0)"
    # Minimum pixel distance required to trigger resize animations.
    # Default: 10
    threshold_px: 10
    # surrogate_color: "#1a1a1a"

  # Animation settings for when a new window appears. Windows only.
  window_open:
    enabled: true
    duration_ms: 150
    # See 'window_move.easing' for available options.
    easing: "cubic_bezier(0.16, 1.0, 0.3, 1.0)"
    # Transition style. Options:
    #   'slide_right' (default)  - slides in from the right
    #   'slide_left'             - slides in from the left
    #   'slide_top'              - slides in from the top
    #   'slide_bottom'           - slides in from the bottom
    #   'fade'                   - no slide; combine with opacity_from for pure fade
    #   'zoom'                   - zoom in from window center
    #   'wipe_right'             - reveal left→right (stationary window)
    #   'wipe_left'              - reveal right→left (stationary window)
    #   'wipe_top'               - reveal bottom→top (stationary window)
    #   'wipe_bottom'            - reveal top→bottom (stationary window)
    style: "slide_right"
    # Starting opacity (0.0–1.0). At 1.0 no fade is applied; at 0.0 the
    # window fades in from fully transparent. Can be combined with any style.
    opacity_from: 1.0

  # Animation settings for workspace-switch transitions.
  workspace_switch:
    enabled: true
    duration_ms: 300
    # See 'window_move.easing' for available options.
    easing: "ease_out_cubic"
    # Transition style. Options:
    #   'slide_horizontal' (default)   - slide left/right, full opacity
    #   'slide_vertical'               - slide up/down, full opacity
    #   'slide_crossfade_horizontal'   - horizontal slide, both sides fade
    #   'slide_crossfade_vertical'     - vertical slide, both sides fade
    #   'slide_fade_out_horizontal'    - horizontal slide, outgoing fades out
    #   'slide_fade_out_vertical'      - vertical slide, outgoing fades out
    #   'slide_fade_in_horizontal'     - horizontal slide, incoming fades in
    #   'slide_fade_in_vertical'       - vertical slide, incoming fades in
    #   'fade'                         - pure crossfade, no slide
    #   'zoom'                         - outgoing shrinks to center, incoming expands
    style: "slide_horizontal"
    # Amount of workspace-level zoom applied during slide transitions. The
    # outgoing workspace shrinks from 1.0 to (1 - zoom_factor) as it exits;
    # the incoming grows from (1 - zoom_factor) to 1.0. Scaling is from the
    # monitor center so all windows move inward together. Range: 0.0 (no zoom)
    # to 1.0 (collapses to a point). Has no effect on 'fade' or 'zoom' styles.
    zoom_factor: 0.1

  # Animation settings for when a window is closed (Windows only).
  window_close:
    enabled: true
    duration_ms: 150
    # See 'window_move.easing' for available options.
    easing: "ease_in_cubic"
    # Transition style. Options:
    #   'fade' (default)         - fade out only
    #   'zoom'                   - zoom out from window center while fading
    #   'slide_right'            - slide off the right edge while fading
    #   'slide_left'             - slide off the left edge while fading
    #   'slide_top'              - slide off the top edge while fading
    #   'slide_bottom'           - slide off the bottom edge while fading
    #   'wipe_right'             - conceal left→right (stationary window)
    #   'wipe_left'              - conceal right→left (stationary window)
    #   'wipe_top'               - conceal bottom→top (stationary window)
    #   'wipe_bottom'            - conceal top→bottom (stationary window)
    style: "fade"
    # Final opacity (0.0–1.0). At 0.0 the window fades to fully transparent.
    opacity_to: 0.0

  # Animation settings for when the focused window changes (Windows only).
  focus_change:
    enabled: false
    duration_ms: 150
    # See 'window_move.easing' for available options.
    easing: "linear"
    # Animation style. Options:
    #   'opacity' (default) - window briefly dims then restores opacity
    #   'scale'             - window briefly pops in from a slightly smaller size
    style: "opacity"
    # For 'scale' style: ratio of the initial shrunken size relative to the
    # window's actual size. E.g. 0.98 = window starts at 98% and grows to 100%.
    # Range: 0.5–1.0.
    scale_factor: 0.98
    # Which focus changes trigger the animation. Options:
    #   'all'            - animate on all focus changes (keyboard and mouse)
    #   'keyboard_only'  - animate only on keyboard/WM-command focus changes
    trigger: "all"

general:
  # Commands to run when the WM has started. This is useful for running a
  # script or launching another application.
  # Example: The below command launches Zebar.
  startup_commands: ["shell-exec zebar"]

  # Commands to run just before the WM is shutdown.
  # Example: The below command kills Zebar.
  shutdown_commands: ["shell-exec taskkill /IM zebar.exe /F"]

  # Commands to run after the WM config is reloaded.
  config_reload_commands: []

  # Whether to automatically focus windows underneath the cursor.
  focus_follows_cursor: false

  # Whether to switch back and forth between the previously focused
  # workspace when focusing the current workspace.
  toggle_workspace_on_refocus: false

  cursor_jump:
    # Whether to automatically move the cursor on the specified trigger.
    enabled: true

    # Trigger for cursor jump:
    # - 'monitor_focus': Jump when focus changes between monitors.
    # - 'window_focus': Jump when focus changes between windows.
    trigger: "monitor_focus"

  # How windows should be hidden when switching workspaces.
  # - 'cloak': (Windows-only) Recommended option for Windows.
  # - 'hide': (Windows-only) Legacy option for Windows. Has stability issues with some apps.
  # - 'place_in_corner': Artifically hides the window by placing it in the corner of the
  #   monitor. On macOS, this is always used instead of cloak/hide.
  hide_method: "cloak"

  # Affects which windows get shown in the native Windows taskbar. Has no
  # effect if `hide_method: 'hide'`.
  # - 'true': Show all windows (regardless of workspace).
  # - 'false': Only show windows from the currently shown workspaces.
  show_all_in_taskbar: false

gaps:
  # Whether to scale the gaps with the DPI of the monitor.
  scale_with_dpi: true

  # Gap between adjacent windows.
  inner_gap: "20px"

  # Gap between windows and the screen edge.
  outer_gap:
    top: "60px"
    right: "20px"
    bottom: "20px"
    left: "20px"

window_effects:
  # Visual effects to apply to the focused window.
  focused_window:
    # Highlight the window with a colored border.
    # ** Exclusive to Windows 11 due to API limitations.
    border:
      enabled: true
      color: "#8dbcff"

    # Remove the title bar from the window's frame. Note that this can
    # cause rendering issues for some applications.
    hide_title_bar:
      enabled: false

    # Change the corner style of the window's frame.
    # ** Exclusive to Windows 11 due to API limitations.
    corner_style:
      enabled: false
      # Allowed values: 'square', 'rounded', 'small_rounded'.
      style: "square"

    # Change the transparency of the window.
    transparency:
      enabled: false
      # Can be something like '95%' or '0.95' for slightly transparent windows.
      # '0' or '0%' is fully transparent (and, by consequence, unfocusable).
      opacity: "95%"

  # Visual effects to apply to non-focused windows.
  other_windows:
    border:
      enabled: true
      color: "#a1a1a1"
    hide_title_bar:
      enabled: false
    corner_style:
      enabled: false
      style: "square"
    transparency:
      enabled: false
      opacity: "0%"

window_behavior:
  # New windows are created in this state whenever possible.
  # Allowed values: 'tiling', 'floating'.
  initial_state: "tiling"

  # Sets the default options for when a new window is created. This also
  # changes the defaults for when the state change commands, like
  # `set-floating`, are used without any flags.
  state_defaults:
    floating:
      # Whether to center floating windows by default.
      centered: true

      # Whether to show floating windows as always on top.
      shown_on_top: false

    fullscreen:
      # Maximize the window if possible. If the window doesn't have a
      # maximize button, then it'll be fullscreen'ed normally instead.
      maximized: false

      # Whether to show fullscreen windows as always on top.
      shown_on_top: false

workspaces:
  - name: "1"
  - name: "2"
  - name: "3"
  - name: "4"
  - name: "5"
  - name: "6"
  - name: "7"
  - name: "8"
  - name: "9"

window_rules:
  - commands: ["ignore"]
    match:
      # Ignores any Zebar windows.
      - window_process: { equals: "zebar" }

      # Ignores picture-in-picture windows for browsers.
      - window_title: { regex: "[Pp]icture.in.[Pp]icture" }
        window_class: { regex: "Chrome_WidgetWin_1|MozillaDialogClass" }

      # Ignore rules for various 3rd-party apps.
      - window_process: { equals: "PowerToys" }
        window_class: { regex: 'HwndWrapper\[PowerToys\.PowerAccent.*?\]' }
      - window_title: { equals: "Command Palette" }
        window_class: { equals: "WinUIDesktopWin32WindowClass" }
      - window_process: { equals: "PowerToys" }
        window_title: { regex: ".*? - Peek" }
      - window_process: { equals: "Lively" }
        window_class: { regex: "HwndWrapper" }
      - window_process: { equals: "EXCEL" }
        window_class: { not_regex: "XLMAIN" }
      - window_process: { equals: "WINWORD" }
        window_class: { not_regex: "OpusApp" }
      - window_process: { equals: "POWERPNT" }
        window_class: { not_regex: "PPTFrameClass" }
      - window_process: { equals: "CleanShot X" }

binding_modes:
  # When enabled, the focused window can be resized via arrow keys or HJKL.
  - name: "resize"
    keybindings:
      - commands: ["resize --width -2%"]
        bindings: ["h", "left"]
      - commands: ["resize --width +2%"]
        bindings: ["l", "right"]
      - commands: ["resize --height +2%"]
        bindings: ["k", "up"]
      - commands: ["resize --height -2%"]
        bindings: ["j", "down"]
      # Press escape/enter/alt+r to return to default keybindings.
      - commands: ["wm-disable-binding-mode --name resize"]
        bindings: ["escape", "enter", "alt+r"]

keybindings:
  # Shift focus in a given direction.
  - commands: ["focus --direction left"]
    bindings: ["alt+h", "alt+left"]
  - commands: ["focus --direction right"]
    bindings: ["alt+l", "alt+right"]
  - commands: ["focus --direction up"]
    bindings: ["alt+k", "alt+up"]
  - commands: ["focus --direction down"]
    bindings: ["alt+j", "alt+down"]

  # Move focused window in a given direction.
  - commands: ["move --direction left"]
    bindings: ["alt+shift+h", "alt+shift+left"]
  - commands: ["move --direction right"]
    bindings: ["alt+shift+l", "alt+shift+right"]
  - commands: ["move --direction up"]
    bindings: ["alt+shift+k", "alt+shift+up"]
  - commands: ["move --direction down"]
    bindings: ["alt+shift+j", "alt+shift+down"]

  # Resize focused window by a percentage or pixel amount.
  - commands: ["resize --width -2%"]
    bindings: ["alt+u"]
  - commands: ["resize --width +2%"]
    bindings: ["alt+p"]
  - commands: ["resize --height +2%"]
    bindings: ["alt+o"]
  - commands: ["resize --height -2%"]
    bindings: ["alt+i"]

  # As an alternative to the resize keybindings above, resize mode enables
  # resizing via arrow keys or HJKL. The binding mode is defined above with
  # the name 'resize'.
  - commands: ["wm-enable-binding-mode --name resize"]
    bindings: ["alt+r"]

  # Disables window management and all other keybindings until alt+shift+p
  # is pressed again.
  - commands: ["wm-toggle-pause"]
    bindings: ["alt+shift+p"]

  # Change tiling direction. This determines where new tiling windows will
  # be inserted.
  - commands: ["toggle-tiling-direction"]
    bindings: ["alt+v"]

  # Change focus from tiling windows -> floating -> fullscreen.
  - commands: ["wm-cycle-focus"]
    bindings: ["alt+space"]

  # Change the focused window to be floating.
  - commands: ["toggle-floating --centered"]
    bindings: ["alt+shift+space"]

  # Change the focused window to be tiling.
  - commands: ["toggle-tiling"]
    bindings: ["alt+t"]

  # Change the focused window to be fullscreen.
  - commands: ["toggle-fullscreen"]
    bindings: ["alt+f"]

  # Minimize focused window.
  - commands: ["toggle-minimized"]
    bindings: ["alt+m"]

  # Close focused window.
  - commands: ["close"]
    bindings: ["alt+shift+q"]

  # Kill GlazeWM process safely.
  - commands: ["wm-exit"]
    bindings: ["alt+shift+e"]

  # Re-evaluate configuration file.
  - commands: ["wm-reload-config"]
    bindings: ["alt+shift+r"]

  # Redraw all windows.
  - commands: ["wm-redraw"]
    bindings: ["alt+shift+w"]

  # Launch CMD terminal. Alternatively, use `shell-exec wt` or
  # `shell-exec %ProgramFiles%/Git/git-bash.exe` to start Windows
  # Terminal and Git Bash respectively.
  - commands: ["shell-exec cmd"]
    bindings: ["alt+enter"]

  # Focus the next/previous active workspace defined in `workspaces` config.
  - commands: ["focus --next-active-workspace"]
    bindings: ["alt+s"]
  - commands: ["focus --prev-active-workspace"]
    bindings: ["alt+a"]

  # Focus the workspace that last had focus.
  - commands: ["focus --recent-workspace"]
    bindings: ["alt+d"]

  # Change focus to a workspace defined in `workspaces` config.
  - commands: ["focus --workspace 1"]
    bindings: ["alt+1"]
  - commands: ["focus --workspace 2"]
    bindings: ["alt+2"]
  - commands: ["focus --workspace 3"]
    bindings: ["alt+3"]
  - commands: ["focus --workspace 4"]
    bindings: ["alt+4"]
  - commands: ["focus --workspace 5"]
    bindings: ["alt+5"]
  - commands: ["focus --workspace 6"]
    bindings: ["alt+6"]
  - commands: ["focus --workspace 7"]
    bindings: ["alt+7"]
  - commands: ["focus --workspace 8"]
    bindings: ["alt+8"]
  - commands: ["focus --workspace 9"]
    bindings: ["alt+9"]

  # Move the focused window's parent workspace to a monitor in a given
  # direction.
  - commands: ["move-workspace --direction left"]
    bindings: ["alt+shift+a"]
  - commands: ["move-workspace --direction right"]
    bindings: ["alt+shift+f"]
  - commands: ["move-workspace --direction up"]
    bindings: ["alt+shift+d"]
  - commands: ["move-workspace --direction down"]
    bindings: ["alt+shift+s"]

  # Move focused window to a workspace defined in `workspaces` config.
  - commands: ["move --workspace 1", "focus --workspace 1"]
    bindings: ["alt+shift+1"]
  - commands: ["move --workspace 2", "focus --workspace 2"]
    bindings: ["alt+shift+2"]
  - commands: ["move --workspace 3", "focus --workspace 3"]
    bindings: ["alt+shift+3"]
  - commands: ["move --workspace 4", "focus --workspace 4"]
    bindings: ["alt+shift+4"]
  - commands: ["move --workspace 5", "focus --workspace 5"]
    bindings: ["alt+shift+5"]
  - commands: ["move --workspace 6", "focus --workspace 6"]
    bindings: ["alt+shift+6"]
  - commands: ["move --workspace 7", "focus --workspace 7"]
    bindings: ["alt+shift+7"]
  - commands: ["move --workspace 8", "focus --workspace 8"]
    bindings: ["alt+shift+8"]
  - commands: ["move --workspace 9", "focus --workspace 9"]
    bindings: ["alt+shift+9"]
