general:
  # Whether to automatically focus windows underneath the cursor.
  focus_follows_cursor: false

  # Whether to jump the cursor between windows focused by the WM.
  cursor_follows_focus: false

  # Whether to switch back and forth between the previously focused
  # workspace when focusing the current workspace.
  toggle_workspace_on_refocus: true

gaps:
  # Gap between adjacent windows.
  inner_gap: '20px'

  # Gap between windows and the screen edge.
  outer_gap: '20px'

# Visual effects to apply to windows. Separate effects can be configured
# for the focused window vs. other windows.
window_effects:
  focused_window:
    # Highlight the window with a colored border. Exclusive to Windows 11.
    # Set to `null` to disable the effect.
    border_color: '#0000ff'

  other_windows:
    border_color: null

window_state_defaults:
  floating:
    # Whether to center floating windows by default.
    centered: true

    # Whether to show floating windows as always on top.
    show_on_top: false

  fullscreen:
    # Whether to prefer fullscreen windows to be maximized.
    maximized: true

    # Whether to show fullscreen windows as always on top.
    show_on_top: false

    # Whether to remove the window's title bar when fullscreen.
    remove_title_bar: false

workspaces:
  - name: '1'
  - name: '2'
  - name: '3'
  - name: '4'
  - name: '5'
  - name: '6'
  - name: '7'
  - name: '8'
  - name: '9'

window_rules:
  # Task Manager requires admin privileges to manage and should be ignored
  # unless running the WM as admin.
  - commands: ['ignore']
    match_process_name: '/Taskmgr|ScreenClippingHost/'

  # Launches system dialogs as floating by default (e.g. File Explorer save
  # and open dialog).
  - commands: ['set-floating']
    match_class_name: '#32770'

  # Do not manage picture-in-picture windows for browsers. Note that the
  # default is for English; change `match_title` to the window's title if
  # you use another language.
  - commands: ['ignore']
    match_title: '[Pp]icture.in.[Pp]icture'
    match_class_name: 'Chrome_WidgetWin_1|MozillaDialogClass'

  # Some applications (e.g. Steam) have borders that extend past the normal
  # border size.
  - commands: ['adjust-borders --right -7px --bottom -7px --left -7px']
    match_process_name: 'steam'

binding_modes:
  - name: 'resize'
    keybindings:
      # Resize focused window by a percentage or pixel amount.
      - commands: ['resize --width -2%']
        bindings: ['h', 'left']
      - commands: ['resize --width +2%']
        bindings: ['l', 'right']
      - commands: ['resize --height +2%']
        bindings: ['k', 'up']
      - commands: ['resize --height -2%']
        bindings: ['j', 'down']
      # Press enter/escape to return to default keybindings.
      - commands: ['wm-disable-binding-mode --name resize']
        bindings: ['escape', 'enter']

keybindings:
  # Shift focus in a given direction.
  - commands: ['focus --dir left']
    bindings: ['alt+h', 'alt+left']
  - commands: ['focus --dir right']
    bindings: ['alt+l', 'alt+right']
  - commands: ['focus --dir up']
    bindings: ['alt+k', 'alt+up']
  - commands: ['focus --dir down']
    bindings: ['alt+j', 'alt+down']

  # Move focused window in a given direction.
  - commands: ['move --dir left']
    bindings: ['alt+shift+h', 'alt+shift+left']
  - commands: ['move --dir right']
    bindings: ['alt+shift+l', 'alt+shift+right']
  - commands: ['move --dir up']
    bindings: ['alt+shift+k', 'alt+shift+up']
  - commands: ['move --dir down']
    bindings: ['alt+shift+j', 'alt+shift+down']

  # Resize focused window by a percentage or pixel amount.
  - commands: ['resize --width -2%']
    bindings: ['alt+u']
  - commands: ['resize --width +2%']
    bindings: ['alt+p']
  - commands: ['resize --height +2%']
    bindings: ['alt+o']
  - commands: ['resize --height -2%']
    bindings: ['alt+i']

  # As an alternative to the resize keybindings above, resize mode enables
  # resizing via HJKL or arrow keys. The binding mode is defined above with
  # the name 'resize'.
  - commands: ['wm-enable-binding-mode --name resize']
    bindings: ['alt+r']

  # Change tiling direction. This determines where new tiling windows will
  # be inserted.
  - commands: ['toggle-tiling-direction']
    bindings: ['alt+v']

  # Change focus between tiling windows and floating/fullscreen windows.
  - commands: ['wm-toggle-focus-state']
    bindings: ['alt+space']

  # Change the focused window to be floating.
  - commands: ['toggle-floating --centered']
    bindings: ['alt+shift+space']

  # Change the focused window to be fullscreen.
  - commands: ['toggle-fullscreen']
    bindings: ['alt+f']

  # Minimize focused window.
  - commands: ['toggle-minimized']
    bindings: ['alt+m']

  # Close focused window.
  - commands: ['close']
    bindings: ['alt+shift+q']

  # Kill GlazeWM process safely.
  - commands: ['wm-exit']
    bindings: ['alt+shift+e']

  # Re-evaluate configuration file.
  - commands: ['wm-reload-config']
    bindings: ['alt+shift+r']

  # Redraw all windows.
  - commands: ['wm-redraw']
    bindings: ['alt+shift+w']

  # Launch CMD terminal. Alternatively `shell_exec --command wt` or
  # `shell_exec --command %ProgramFiles%/Git/git-bash.exe` to start Windows
  # Terminal and Git Bash respectively.
  - commands: ['shell-exec --command cmd']
    bindings: ['alt+enter']

  # Focus the workspace that last had focus.
  - commands: ['focus --recent-workspace']
    bindings: ['alt+y']

  # Focus the next/previous workspace defined in `workspaces` config.
  - commands: ['focus --next-workspace']
    bindings: ['alt+t']
  - commands: ['focus --prev-workspace']
    bindings: ['alt+shift+t']

  # Change focus to a workspace defined in `workspaces` config.
  - commands: ['focus --workspace 1']
    bindings: ['alt+1']
  - commands: ['focus --workspace 2']
    bindings: ['alt+2']
  - commands: ['focus --workspace 3']
    bindings: ['alt+3']
  - commands: ['focus --workspace 4']
    bindings: ['alt+4']
  - commands: ['focus --workspace 5']
    bindings: ['alt+5']
  - commands: ['focus --workspace 6']
    bindings: ['alt+6']
  - commands: ['focus --workspace 7']
    bindings: ['alt+7']
  - commands: ['focus --workspace 8']
    bindings: ['alt+8']
  - commands: ['focus --workspace 9']
    bindings: ['alt+9']

  # Move the focused window's parent workspace to a monitor in a given
  # direction.
  - commands: ['move-workspace --dir left']
    bindings: ['alt+shift+a']
  - commands: ['move-workspace --dir right']
    bindings: ['alt+shift+f']
  - commands: ['move-workspace --dir up']
    bindings: ['alt+shift+d']
  - commands: ['move-workspace --dir down']
    bindings: ['alt+shift+s']

  # Move focused window to a workspace defined in `workspaces` config.
  - commands: ['move --workspace 1', 'focus --workspace 1']
    bindings: ['alt+shift+1']
  - commands: ['move --workspace 2', 'focus --workspace 2']
    bindings: ['alt+shift+2']
  - commands: ['move --workspace 3', 'focus --workspace 3']
    bindings: ['alt+shift+3']
  - commands: ['move --workspace 4', 'focus --workspace 4']
    bindings: ['alt+shift+4']
  - commands: ['move --workspace 5', 'focus --workspace 5']
    bindings: ['alt+shift+5']
  - commands: ['move --workspace 6', 'focus --workspace 6']
    bindings: ['alt+shift+6']
  - commands: ['move --workspace 7', 'focus --workspace 7']
    bindings: ['alt+shift+7']
  - commands: ['move --workspace 8', 'focus --workspace 8']
    bindings: ['alt+shift+8']
  - commands: ['move --workspace 9', 'focus --workspace 9']
    bindings: ['alt+shift+9']
